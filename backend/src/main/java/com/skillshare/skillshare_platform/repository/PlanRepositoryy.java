package com.skillshare.skillshare_platform.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.skillshare.skillshare_platform.entity.Plann;

public interface PlanRepositoryy extends JpaRepository<Plann, Long> {
}
